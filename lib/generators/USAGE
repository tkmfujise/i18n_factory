Description:
    Generate i18n files for each model.

Example:
    bin/rails generate i18n_factory:update ModelName
    or
    bin/rails generate i18n_factory:update_all

    This will create:
        config/locales/<model_name>/<your_locale>.yml
