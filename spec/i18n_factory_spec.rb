# frozen_string_literal: true

RSpec.describe I18nFactory do
  it "has a version number" do
    expect(I18nFactory::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
